program ad;

type
    nota=0..10;
var
    hola: nota;

begin
    read(hola);
    writeln(hola);

    // hola:=0;
    // repeat
    //     hola:=hola + 1;
    //     writeln(hola);
    // until (hola = 30);
    // while(hola<>30)do
    //     hola:=hola + 1;
    //     writeln(hola);
end.
